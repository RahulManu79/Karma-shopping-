<%- include('includes/header.ejs') %>

<div class="text-center mx-auto mt-2"  style="min-width: 22rem;"></div>

<div class="container-fluid mt-3" style="min-height: 33rem;">
  <h2 class="text-center text-white">Manage Orders</h2>
  <!-- <div class="d-flex justify-content-between">
    <input class="form-control w-50 mx-auto mb-3" type="search" placeholder="Search" aria-label="Search" id="myInput">
  </div> -->
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col">Order ID</th>
        <th>Name</th>
        <!-- <th>Email</th> -->
        <th>Amount</th>
        <th>Payment Mode</th>
        <th>paymentStatus</th>
        <!-- <th>Status</th> -->
        <th>Track</th>
        <th></th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody id="myTable">
      
      <% for(result of result) { %>
             
       
   
      <tr style="height: 50px !important;" class="text-wrap">
        <th scope="row"><%= result._id %> </th>
        <td><%= result.address.firstName %> </td>
        <td>₹ <%= result.products[0].quantity * result.products[0].price  %> </td>
        <td><%= result.paymentMethod %> </td>
        <td><%= result.paymentStatus %> </td>
        <!-- <td>
          
          <label class="badge badge-success">Delivered</label>
          
          <label class="badge badge-danger">Cancelled</label>
          
          <label class="badge badge-warning" type="button"  data-toggle="modal" data-target="#myModal{{order.id}}" id="Modal_button">Change</button>
            
          </td> -->
          <td>
            <select style="background-color: yellow ; text-size-adjust: 12px; "data-oid="<%= result._id %>"  name="track" id="track">     
              
              <% if (result.orderStatus == 'Cancellede') { %>
                <% } else{%>
                  <% } %> 
                  <option value="<%= result.track %>"><%= result.track %> </option>
              <option value="Cancellede">Cancellede</option>
              <option value="orderconfirmed" >orderconfirmed</option>
            <option value="Shipped"  >Shipped</option>
            <option value="Delivered">Delivered</option>
          </select>
          </td>
        <td>
        
          
          <button type="button" id="changetrack" class="btn btn-primary btn-sm" onclick="orderStatus(this)">
            Change
          </button>         

          
        </td>
      
        <td>
          <button class="btn btn-primary btn-sm" type="button">veiw Details</button>
        </td>

      </tr>

      <!-- Button trigger modal -->

<!-- Status Modal -->
<!-- <div class="modal fade" id="myModal{{order.id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5>Change Status</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form action="{% url 'update_order' order.id %}" method="POST">
          

          <div class="row mb-2">
            <input class="ml-3" type="radio" name="status" value="Order Confirmed"  checked>
            <label for="dreamweaver" class="ml-2 mt-2">Order Confirmed</label>
          </div>
          <div class="row mb-2">
            <input class="ml-3" type="radio" name="status" value="Shipped">
            <label for="dreamweaver" class="ml-2 mt-2 text-info">Shipped</label>
          </div>
          <div class="row mb-2">
            <input class="ml-3" type="radio" name="status" value="Out for delivery">
            <label for="dreamweaver" class="ml-2 mt-2 text-warning">Out for delivery</label>
          </div>
          <div class="row mb-2">
            <input class="ml-3" type="radio" name="status" value="Delivered">
            <label for="dreamweaver" class="ml-2 mt-2 text-success">Delivered</label>
          </div>

          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button type="submit" class="btn btn-primary">Save changes</button>
          </div>
        </form> -->
    </div>
  </div>
</div>

          <!--Cancel Modal -->
          <!-- <div class="modal fade" id="cancel{{order.id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <h5 class="modal-title" id="exampleModalLabel">Cancel Order</h5>
                  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                  </button>
                </div>
                <div class="modal-body text-left">
                  Are you sure to cancel the order <b>#</b>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-dismiss="modal">No</button>
                  <a type="button" href="{% url 'cancel_order' order.order_number %}" class="btn btn-primary">Yes</a>
                </div>
              </div>
            </div>
          </div>

          
         -->
          <% } %> 
        
    </tbody>
  </table>
</div>

<!-- <nav aria-label="Page navigation example" class="mt-3">
  <ul class="pagination d-flex justify-content-center">
     -->
    <!-- <li class="page-item">
      <a class="page-link" href="?page={{ orders.previous_page_number }}" aria-label="Previous">
        <span aria-hidden="true">&laquo;</span>
        <span class="sr-only">Previous</span>
      </a>
    </li> -->
    
    <!-- <li class="page-item"><a class="page-link" href="?page={{ orders.number }}"></a></li>
    
    <li class="page-item"><a class="page-link" href="?page={{ orders.next_page_number }}"></a></li> -->

<%- include('includes/footer.ejs') %>


<script>
  function orderStatus(e){
  
  const oid = $('#track').data('oid')
  const value = $('#track').find(":selected").val();
  alert(`oid = ${oid}`)
  alert(`value = ${value}`)
    
$.ajax({
  url:"/admin/order-status",
  data:{
    oid,
    value
  },
  method:"post",
  success:(response)=>{
   if(response.status){

    Swal.fire({
  position: 'top-end',
  icon: 'success',
  title: 'Track statuse has been changed',
  showConfirmButton: false,
  timer: 1000
})
    
   }

  }

})
    
}

</script>